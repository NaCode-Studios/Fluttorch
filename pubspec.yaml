name: _fluttorch_workspace
publish_to: none

environment:
  sdk: ^3.9.0

workspace:
  - packages/fluttorch
  - packages/fluttorch_executorch
  - packages/fluttorch_gen
  - packages/fluttorch_test

dev_dependencies:
  lints: ^6.0.0
