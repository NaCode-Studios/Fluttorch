name: voltacast_example
description: A day-ahead electricity demand forecast, exported from PyTorch and measured on device.
publish_to: none

environment:
  sdk: ^3.9.0

resolution: workspace

dependencies:
  fluttorch: ^0.7.0

dev_dependencies:
  build_runner: ^2.16.0
  fluttorch_gen: ^0.7.0
  fluttorch_test: ^0.7.0
  lints: ^6.0.0
  test: ^1.25.0
